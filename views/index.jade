//
   Created by Jiangda on 2015/7/5.

include header
each post, index in posts
    p
        h2
            a(href='/#{post.name.name}/#{post.time.day}/#{post.title}')=post.title
    p.info
        p 作者:
         a(href='/u/#{post.name.name}')= post.name.name
         |  日期:#{post.time.day}
    p= post.post
include footer